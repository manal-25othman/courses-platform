/**
 * Phase 0 placeholder.
 *
 * No product interface exists yet — screens are built from Phase 1 onward,
 * once their requirements are approved. This page only confirms the web app
 * builds and runs.
 */
export default function HomePage() {
  return (
    <main
      style={{
        fontFamily: 'system-ui, sans-serif',
        maxWidth: '32rem',
        margin: '4rem auto',
        padding: '0 1.5rem',
        lineHeight: 1.6,
      }}
    >
      <h1 style={{ fontSize: '1.5rem', marginBottom: '0.5rem' }}>TOP GOAL</h1>
      <p style={{ color: '#555' }}>Interactive English Learning Platform</p>
      <p style={{ color: '#777', fontSize: '0.875rem', marginTop: '2rem' }}>
        Phase 0 — foundation only. No features are built yet.
      </p>
    </main>
  );
}
